// Re-exports dark defaults for any static (non-themed) usage.
// In components, prefer: import { useTheme } from '../contexts/ThemeContext'
export { darkPalette as palette, darkStatusColors as statusColors, darkSignalDefs as signalDefs } from "./themes";
